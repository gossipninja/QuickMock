<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android.permission.ACCESS_MOCK_LOCATION" />
    <uses-permission android.permission.FOREGROUND_SERVICE" />
    <uses-permission android.permission.FOREGROUND_SERVICE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="QuickMock"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <service
            android:name=".MockService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <receiver
            android:name=".MockWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.location.PROVIDERS_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mock_info" />
        </receiver>

    </application>
</manifest>
